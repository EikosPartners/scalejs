import base from './scalejs.base';

let { is, typeOf } = base.type;
let { has, valueOrDefault, get, merge } = base.object;

export {
    is,
    typeOf,
    has,
    valueOrDefault,
    get,
    merge
}
