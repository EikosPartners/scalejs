import core from "./scalejs.core";
import application from "./scalejs.application";
import sandbox from "./scalejs.sandbox";


export default {
    core: core,
    application: application,
    sandbox: sandbox
}